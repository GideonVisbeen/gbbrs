import React from 'react';
import { render } from 'react-dom';

import IconPage from './components/IconPage.js';

const element = <IconPage />;

render(element,document.getElementById('react_app'));