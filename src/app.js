import React from 'react';
import { render } from 'react-dom';

const element = <h1>gulp, browserify, sass, compass, es2015, react workflow boilerplate</h1>;

render(element,document.getElementById('react_app'));